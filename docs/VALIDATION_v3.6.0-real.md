# Validation — v3.6.0-real

Validation should include compile checks, pytest, version checks, startup smoke, v3 release validation, v3 UX validation, screenshot dry-run, package cleanliness, and secret scan. Freshness/scheduler workflows are read-only, non-autonomous by default, and do not arm live trading.
