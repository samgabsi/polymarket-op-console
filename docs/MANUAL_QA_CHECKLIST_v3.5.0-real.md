# Manual QA Checklist — v3.5.0-real

- Open `/v3/datasets` and confirm the Dataset Builder page renders.
- Confirm read-only labels are visible.
- Confirm snapshot table empty state is readable.
- Run safe demo data locally and confirm fake dataset records appear.
- Build a dataset manifest and confirm quality/provenance are shown.
- Export JSON, Markdown, snapshot CSV, quality CSV, and provenance CSV.
- Confirm no secrets appear in UI or exports.
- Confirm no dataset button places or cancels orders.
- Confirm `/v3`, `/v3/search`, `/v3/graph`, `/v3/analytics`, `/v3/simulation`, and `/v2-live/*` still render.
