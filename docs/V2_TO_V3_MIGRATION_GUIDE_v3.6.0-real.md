# V2 to V3 Migration Guide — v3.6.0-real

Existing v2 routes and safety controls remain compatible. v3.6 adds `/v3/freshness` and `/api/v3/freshness/*` while preserving v3 datasets, simulation, analytics, workflows, search, graph, and command center.
