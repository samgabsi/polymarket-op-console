# Visual QA Checklist — v3.5.0-real

Review `/v3/simulation`, `/v3/simulation/replay`, `/v3/simulation/sessions`, `/v3/simulation/pre-trade`, `/v3/simulation/thesis`, `/v3/simulation/alerts`, `/v3/simulation/portfolio`, `/v3/simulation/governance`, `/v3/simulation/no-trade`, and `/v3/simulation/reports`.

Check: simulation labels, no-live-order banners, readable tables, assumptions panel, unknown/unavailable states, report exports, responsive/narrow width behavior, keyboard focus, no secrets visible, and no misleading financial-advice language.


## v3.5 Dataset Visual QA

- `/v3/datasets` dashboard cards are readable.
- Snapshot collector, dataset manifests, quality, provenance, and replay readiness panels render.
- Read-only labels are obvious.
- Empty states are clear.
- Dataset exports are easy to find.
- No data collection or dataset build runs on page load.
